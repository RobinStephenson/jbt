using UnityEngine;
using System.Collections;

public class Temp : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Inventory inv = new Inventory(3, 4, 3, 4);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
